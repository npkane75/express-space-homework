<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Missions</title>
</head>
<body>
    <h1>Show Missions page</h1>
    <p>The <%= indMissions.name %> is <%= indMissions.launchDate %><%= indMissions.operator %><%= indMissions.missionType %>.</p>
    <% if(indMissions.img === true){ %>
        <p>There are no images yet from this mission</p>
    <% } else { %>
        <p>These images are now declassified</p>
    <% } %>
</body>
</html>